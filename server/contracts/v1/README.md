# CQI Contract v1

Frozen response contracts for admin APIs:

- `GET /analysis/admin/filter-options`
- `GET /analysis/admin/co-po-insights`

Use `contract_version = cqi.v1` for frontend compatibility checks.
