/**
 * Computes Attendance metric.
 * Pure function.
 * 
 * @param {Object} data - Contains STUDENT_ATTENDANCE, ATTENDANCE_RULE
 * @returns {Object} Computed weighted attendance score
 */
function computeAttendance(data) {
  // TODO: Implement Attendance logic
  
  return {
    student_id: 1,
    weighted_score: 0.0
  };
}

module.exports = {
  computeAttendance
};
